[project]
name = "zoco-agents"
version = "0.1.0"
description = "An experimental, open-source foundation for building agentic AI systems in Python."
readme = "README.md"
requires-python = ">=3.12.3"
dependencies = [
    "openai>=2.46.0",
]
